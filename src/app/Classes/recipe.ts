export class Recipe {
    constructor(protected name: string, protected description: string, protected image: string, protected ingredients: string, protected category: string, protected rating: number, protected country: string) { }
}
